name: Keep Render Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app health endpoint
        run: curl -fsS --max-time 180 https://ai-loan-underwriting-assistant.onrender.com/health
